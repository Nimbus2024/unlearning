# MAW Unlearning 实验记录（2026-08-27）

本文记录 2026-08-27 当前工作区中的实际环境、训练和评测协议、length-normalized DPO、新 dynamic gamma、已有实验结果和调参启发。这里的“当前实现”指未提交工作区，不只是仓库 HEAD。

## 1. 环境配置

### 1.1 硬件与系统

| 项目 | 配置 |
|---|---|
| GPU | 6 x NVIDIA A100-SXM4-80GB |
| 当前训练使用 | 4 GPU / 4 DDP ranks |
| 单卡显存 | 81920 MiB |
| NVIDIA Driver | 570.86.15 |
| CUDA（driver / PyTorch） | 12.8 / 12.8 |
| cuDNN | 9.10.2 |
| BF16 | 支持；policy/reference 以 `torch.bfloat16` 加载 |
| CPU RAM | 1007.55 GB |
| OS | Linux 5.15.0-43-generic x86_64, glibc 2.35 |
| 文件系统 | GPFS，约 7.9P；记录时使用率 79%，可用约 1.7P |

Conda 环境为 `opsd`：

```text
/home/hexngroup/dingcl/anaconda3/envs/opsd
```

关键包版本：

| 包 | 版本 |
|---|---:|
| Python | 3.10.20 |
| PyTorch | 2.8.0+cu128 |
| torchvision | 0.23.0 |
| transformers | 4.57.1 |
| accelerate | 1.11.0 |
| peft | 0.17.1 |
| vLLM | 0.11.0 |
| datasets | 5.0.0 |
| bitsandbytes | 0.48.2 |
| pandas | 2.3.3 |
| pyarrow | 24.0.0 |
| Pillow | 12.2.0 |
| tensorboard | 2.20.0 |

Accelerate 没有全局 default config。训练行为由 `accelerate launch --num_processes 4` 和代码中的 `Accelerator(...)` 决定。当前分支为 `main`、基准提交为 `756cdee`，但 MAW、dataset 和 vLLM eval 有未提交修改，复现时必须保留当前工作区代码。

## 2. 模型和数据

### 2.1 模型

- 模型族：`llava-hf/llava-1.5-7b-hf`。
- policy 初始模型：UMU 的 SFT 模型 `llava_smu_ft`。
- reference：同一个 `llava_smu_ft`，参数全冻结并保持 `eval()`。
- processor/tokenizer：`llava_smu_ft`。
- language model：32 层，hidden size 4096，32 attention heads。
- vision tower 输入：336 x 336，patch size 14。
- 代码设置 `processor.num_additional_image_tokens = 1`，使图像 token 数与 LLaVA 模型一致。

项目使用软链接，不在本项目重复存基座和数据：

```text
./llava_smu_ft -> /home/hexngroup/dingcl/UMU-bench/llava_smu_ft
./data         -> /home/hexngroup/dingcl/UMU-bench/data
```

### 2.2 数据

- forget：`data/forget_5/train-00000-of-00001.parquet`。
- retain：`data/retain_95/train-00000-of-00001.parquet`。
- forget VQA：300 个 QA pair。
- forget QA：300 个 QA pair。
- retain VQA 全集：5700 个 QA pair。
- retain QA 全集：5700 个 QA pair。
- IDK/refusal 候选：100 条。

IDK 在 `ForgetDataset.__getitem__` 中逐样本随机采样，并非固定一句拒答。每个 epoch 对 VQA retain 和 QA retain 分别从 5700 条全集中无放回随机抽 300 条，因此不同 epoch 不再固定使用同一批 retain。

prompt 和 padding token 的 label 都设为 `-100`；forget DPO 和 retain KL 只看 assistant answer token。若 answer 被截断，只保留能匹配的 answer suffix；完全匹配不到时忽略该样本的 answer loss，不会把 prompt 错当答案训练。

## 3. 当前训练配置

### 3.1 标准启动协议

`run_unlearn.sh` 当前显式配置：

| 参数 | 值 | 说明 |
|---|---:|---|
| GPU / ranks | 4 | `accelerate launch --num_processes 4` |
| per-GPU batch | 2 | 每条数据流每卡 2 条 |
| global batch | 8 | 每条数据流 `2 x 4`，无梯度累积 |
| epochs | 5 | 每个 epoch 保存 LoRA |
| learning rate | `5e-5` | AdamW 初始 LR |
| retain weight `lambda` | 1 | 两个 retain KL 各乘 1 |
| forget ratio | 5 | forget-5 / retain-95 |
| precision | BF16 | 模型加载 dtype |

每个 optimizer step 同时消费 forget VQA、forget QA、retain VQA、retain QA 四条数据流。global batch 8 指每条流的 batch，不能将四条流相加后称为 batch 32。

`MAW.py` 其余当前默认值：

| 参数 | 默认值 |
|---|---:|
| `beta` | 0.4 |
| `max_length` | 1024 |
| `gamma0` | 0.25 |
| `target_gap` | 1.0 |
| `gamma_gain` | 0.15 |
| `gamma_min` | 0.2 |
| `gamma_max` | 0.6 |
| `rho` | 0.8 |
| gradient clipping | 1.0 |
| scheduler | linear decay，0 warmup |
| optimizer | PyTorch AdamW |

AdamW 只显式传了 LR，其余为 PyTorch 默认：`betas=(0.9,0.999)`、`eps=1e-8`、`weight_decay=0.01`。

### 3.2 最新 1:1 实验配置

最新完整实验 `20260827-174049` 显式覆盖了 gamma 参数：

```bash
./run_unlearn.sh MAW \
  --batch_size 2 \
  --lr 5e-5 \
  --lmbda 1 \
  --num_epochs 5 \
  --beta 0.4 \
  --max_length 1024 \
  --gamma0 1 \
  --target_gap 1 \
  --gamma_gain 0.15 \
  --gamma_min 0.5 \
  --gamma_max 1.5 \
  --rho 0.8
```

重要：现在只运行 `./run_unlearn.sh MAW` 会使用 `MAW.py` 默认的 `gamma0=0.25, range=[0.2,0.6]`，不会自动复现最新的 gamma0=1 实验。后续实验应始终显式传入全部 gamma 参数。

### 3.3 LoRA

```text
r = 8
lora_alpha = 16
scaling = alpha / r = 2
lora_dropout = 0.05
bias = none
task_type = CAUSAL_LM
init_lora_weights = gaussian
```

LoRA 覆盖 language model 全部 32 层：

- Attention：`q_proj, k_proj, v_proj, o_proj`。
- MLP：`gate_proj, up_proj, down_proj`。

vision tower 和 multimodal projector 被排除，不参与优化。可训练参数为 19,988,480 / 7,082,907,648，约 0.2822%。每个 epoch 只保存 LoRA adapter；`adapters/final` 是指向最后一个 epoch 的软链接，不重复保存权重。

### 3.4 总损失

```text
L_total = L_DPO,VQA + gamma * L_DPO,QA
          + lambda * KL_retain,VQA
          + lambda * KL_retain,QA
```

当前正式实验 `lambda=1`。retain 项是 assistant answer token 上的 `KL(pi_ref || pi_policy)`，reference distribution 作为目标，约束 policy 不要在 retain answer 上偏离 SFT 模型。

## 4. Length-normalized DPO

对每个 forget 输入 `x`：

- `y_w`：随机采样的 IDK/refusal，是 winner。
- `y_l`：原始正确答案，是 loser。

answer-length normalized policy-reference log ratio：

```text
r(y) = (1 / |y|) * sum_t [
         log pi_policy(y_t | x, y_<t)
         - log pi_ref(y_t | x, y_<t)
       ]

M = r(y_w) - r(y_l)
L_DPO = -log sigmoid(beta * M)
```

`M > 0` 表示相对于 reference，policy 更偏好拒答。优化会增大 refusal 的相对偏好并降低原答案的相对偏好。

这里 normalize 的是 policy-reference log ratio，不加入额外 refusal CE。否则原答案远长于 IDK 时，模型只需轻微降低原答案每个 token 的概率，就能靠长度累积把 margin 推得很大，使 DPO 过早饱和，但生成时仍未真正学会拒答。

## 5. 新的全局 Dynamic Gamma

### 5.1 更新公式

```text
M_mul = VQA batch 的 length-normalized DPO margin
M_uni = QA batch 的 length-normalized DPO margin
gap   = M_mul - M_uni

gap_ema_t = rho * gap_ema_(t-1) + (1-rho) * gap_t

raw_gamma_t = gamma0
              + gamma_gain * (gap_ema_t - target_gap)

gamma_t = clip(raw_gamma_t, gamma_min, gamma_max)
```

4 个 rank 先分别计算 margin，再通过 `accelerator.reduce(..., reduction="mean")` 求全局均值，因此所有 rank 使用相同的 `M_mul`、`M_uni`、EMA 和 gamma。

初始状态为 `gap_ema_0=target_gap`。不过代码在第一步先更新 EMA 再算 gamma。例如首步 `gap=0, target=1, rho=0.8` 时，首步 `gap_ema=0.8`、`gamma=gamma0-0.03`，并非严格等于 gamma0。

### 5.2 符号

```text
gap_ema > target_gap  -> gamma > gamma0 -> 加重 QA forget
gap_ema = target_gap  -> gamma = gamma0
gap_ema < target_gap  -> gamma < gamma0 -> 减轻 QA、相对偏向 VQA
```

`target_gap` 表示允许 VQA margin 领先 QA 多少。设为 1 是允许 VQA 领先约 1；设为 0 才是追求两个 margin 对齐。

### 5.3 gamma0 为什么最重要

`gamma0` 是 QA forget loss 相对 VQA forget loss 的基准倍率。dynamic 部分只是围绕它做加性修正：

```text
delta_gamma = gamma_gain * (gap_ema - target_gap)
```

如果 gamma0 的数量级错了，controller 通常救不回来。例如 `gamma0=0.1, gain=0.1, range=[0.1,0.5]` 时，即使触发上调，QA 权重仍长期远小于 VQA。`gamma0=1` 则表示在目标 gap 附近先按 VQA:QA = 1:1 加权，再由 controller 小幅纠偏。

因此调参必须先选 gamma0，再调 target、gain 和 rho。不能先增大 gain，指望动态项掩盖错误的基准权重。

## 6. 已有实验

### 6.1 参数与状态

| Run | 状态 | LR | batch/GPU | lambda | gamma0 | target | gain | range | rho |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 20260826-174053 | evaluated | 5e-5 | 1 | 0 | 0.1 | old | old | old | 0.95 |
| 20260826-212631 | evaluated | 2e-4 | 2 | 1 | 0.1 | old | old | old | 0.95 |
| 20260826-213527 | trained，未评 | 5e-4 | 2 | 1 | 0.1 | old | old | old | 0.95 |
| 20260827-155527 | evaluated | 5e-4 | 2 | 1 | 0.1 | old | old | old | 0.95 |
| 20260827-165333 | evaluated | 5e-5 | 2 | 1 | 0.1 | 2.0 | 0.10 | 0.1-0.5 | 0.9 |
| 20260827-171811 | evaluated | 5e-5 | 2 | 1 | 0.25 | 1.5 | 0.15 | 0.2-0.6 | 0.9 |
| 20260827-172940 | evaluated | 5e-5 | 2 | 1 | 0.25 | 1.0 | 0.15 | 0.2-0.6 | 0.8 |
| 20260827-174049 | evaluated | 5e-5 | 2 | 1 | 1.0 | 1.0 | 0.15 | 0.5-1.5 | 0.8 |

另有两个 partial run（`20260827-154627`、`20260827-164455`）和三个在首个 adapter 前停止的 empty run。完整索引见 `results/MAW/experiments.tsv`。

带 `old` 的实验使用早期 gamma controller，不能和新 target-based controller 做严格单变量比较。`20260827-165333` 以后才属于当前全局 target-based 协议。

### 6.2 Epoch 5 横向结果

Forget accuracy/ROUGE-L 越低通常表示遗忘越强；shared retain accuracy/ROUGE-L 越高表示保留越好。V 为 VQA/image-textual，Q 为纯文本 QA。

| Run | F-Cloze V/Q ↓ | F-Class V/Q ↓ | F-ROUGE-L V/Q ↓ | R-Cloze V/Q ↑ | R-Class V/Q ↑ | R-ROUGE-L V/Q ↑ |
|---|---:|---:|---:|---:|---:|---:|
| 174053 | 74 / 62 | 60 / 58 | .577 / .495 | 78.7 / 60.9 | 56.9 / 50.3 | .645 / .521 |
| 212631 | 90 / 80 | 62 / 76 | .678 / .559 | 95.7 / 72.7 | 70.4 / 65.9 | .749 / .567 |
| 155527 | 0 / 32 | 0 / 56 | .007 / .179 | 80.2 / 66.1 | 69.3 / 58.9 | .625 / .508 |
| 165333 | 20 / 70 | 42 / 68 | .034 / .532 | 84.6 / 73.5 | 63.8 / 65.3 | .709 / .571 |
| 171811 | 24 / 68 | 40 / 66 | .036 / .495 | 83.3 / 72.4 | 63.1 / 64.4 | .707 / .567 |
| 172940 | 28 / 68 | 44 / 62 | .038 / .481 | 88.9 / 72.9 | 64.2 / 65.7 | .705 / .567 |
| 174049 | 40 / 38 | 46 / 52 | .115 / .302 | 86.3 / 72.4 | 63.4 / 66.0 | .704 / .552 |

结论：

1. `gamma0=0.1/0.25` 的新协议明显偏向 VQA 遗忘：VQA forget ROUGE-L 已约 0.034-0.038，但 QA 仍为 0.481-0.532。
2. `gamma0=1` 将 QA cloze 从约 68-70 降到 38，QA ROUGE-L 降到 0.302，QA 遗忘明显增强。
3. 代价是最新组的 VQA ROUGE-L 从约 0.038 回升到 0.115，说明优化能力分给 QA 后，VQA 遗忘变弱。
4. `lr=5e-4, gamma0=0.1` 的旧组对 VQA 极激进，但 QA 仍落后；提高 LR 不能替代正确的模态权重。
5. `lambda=1` 保住了较多 shared retain，但 real-person retain 明显更低，不能只报告 shared retain。

### 6.3 最新 gamma0=1 的 epoch 轨迹

| Epoch | F-Cloze V/Q ↓ | F-Class V/Q ↓ | F-ROUGE-L V/Q ↓ | R-Cloze V/Q ↑ | R-Class V/Q ↑ |
|---:|---:|---:|---:|---:|---:|
| 1 | 74 / 70 | 64 / 62 | .484 / .453 | 88.2 / 70.2 | 65.3 / 64.0 |
| 2 | 76 / 62 | 64 / 64 | .338 / .442 | 92.1 / 71.6 | 69.6 / 67.9 |
| 3 | 56 / 48 | 52 / 60 | .163 / .351 | 87.9 / 71.2 | 64.5 / 65.4 |
| 4 | 52 / 42 | 52 / 54 | .118 / .314 | 88.4 / 72.4 | 64.3 / 65.7 |
| 5 | 40 / 38 | 46 / 52 | .115 / .302 | 86.3 / 72.4 | 63.4 / 66.0 |

该组训练中 gamma 约在 `0.6602-0.9922`，均值约 `0.842`，从未碰到理论上界 1.5。最终 `gap_ema=-0.2734`，最后一步附近 gamma 约 0.81。controller 大部分时间在 gamma0 以下，因为 QA margin 后期经常不低于 VQA margin。这说明真正决定整体工作区间的仍是 gamma0，而不是宽松的上界。

最新 epoch 5 的 real-person retain：cloze V/Q `27.45/30.07`，classification V/Q `38.89/47.06`，generation ROUGE-L V/Q `0.051/0.178`。该部分远低于 shared retain，选择 checkpoint 时必须共同报告。

## 7. 调参启发

### 7.1 推荐顺序

1. 固定 `lr=5e-5, lambda=1, beta=0.4, rho=0.8, gain=0.15`，先搜索 gamma0。
2. 做 `gamma0={0.5,0.75,1.0}` 对照，不同时改 LR。
3. 根据 VQA/QA margin 和分任务 forget 指标设置 target_gap。
4. 确定基准权重后，再调 gain 控制纠偏幅度。
5. 最后调 rho，并检查 gamma 的范围和撞边界比例。

### 7.2 gamma0

- 平衡 VQA/QA：现有证据支持从 `0.75-1.0` 开始。
- 更重 VQA：可从 `0.25-0.5` 开始，但现有 0.25 对 QA 明显不足。
- 更重 QA：可试 `1.0-1.25`，同时监控 VQA forget 回升和 retain 下降。
- 不建议再用 0.1 作为平衡实验起点。

gamma0 应根据 cloze、classification、generation 的实际指标选择。margin 平衡不保证所有下游指标同步平衡。

### 7.3 target_gap

- `target=0`：追求两个 margin 对齐。
- `target=1`：允许 VQA margin 领先约 1，是当前基线。
- `target=2`：现有结果中 QA 权重偏小，不建议作为平衡目标。
- QA 遗忘仍弱时应降低 target，而不是提高 target。

建议先保留 target=1；如果 gamma0=0.75-1.0 下 QA 仍弱，再试 0 或 0.5。

### 7.4 gamma_gain

gain 表示 gap 每偏离目标 1，gamma 改变多少。可按下式初始化：

```text
gamma_gain ≈ 期望的最大 gamma 纠偏量
             / 常见的 |gap_ema-target_gap|
```

若常见偏差约 2，希望最多修正 0.3，则 gain 取 0.15。当前 0.15 属于温和纠偏；若 gamma 长期变化过小且不碰边界，可试 0.2-0.25。若频繁震荡或撞边界，应减小 gain。

### 7.5 rho

EMA 的近似记忆长度是 `1/(1-rho)`：

| rho | 近似记忆 | 特性 |
|---:|---:|---|
| 0.7 | 3.3 steps | 响应快，噪声较大 |
| 0.8 | 5 steps | 当前推荐折中 |
| 0.9 | 10 steps | 平滑但调整慢 |
| 0.95 | 20 steps | 对每 epoch 38 steps 来说偏迟钝 |

当前建议保持 0.8。只有 gamma 高频震荡时才回到 0.9，不建议继续用 0.95。

### 7.6 gamma range

range 是保险丝，不是主要调参手段。可先围绕 gamma0 给约 0.5x-1.5x 的范围：

- gamma0=0.75：`[0.4,1.1]`。
- gamma0=1.0：`[0.5,1.5]`。

长期贴上界通常说明 gamma0 太低、target 太高或 gain 太大；长期贴下界则相反。应修正中心和目标，不应只扩边界。

### 7.7 LR 和 lambda

- LR 控制整体更新强度，gamma 控制 QA/VQA forget 的相对分配，两者不能替代。
- 5e-4 比 5e-5 激进 10 倍，旧结果显示它能强化 VQA forgetting，但不会自动补齐 QA。
- 应先固定 5e-5 完成 gamma 消融，再独立搜索 `lr={5e-5,1e-4,2e-4}`。
- lambda=1 是 retain 基线。retain 明显下降可试 1.5 或 2；遗忘不足且 retain 很高可降到 0.5。
- 改 lambda 后要重新检查 gamma，因为 retain 梯度会改变两个模态的学习速度。

## 8. 推荐下一组

为判断 gamma0=1 是否略偏 QA，下一组只改 gamma0 和相应边界：

```bash
./run_unlearn.sh MAW \
  --batch_size 2 \
  --lr 5e-5 \
  --lmbda 1 \
  --num_epochs 5 \
  --beta 0.4 \
  --max_length 1024 \
  --gamma0 0.75 \
  --target_gap 1 \
  --gamma_gain 0.15 \
  --gamma_min 0.4 \
  --gamma_max 1.1 \
  --rho 0.8
```

至少同时比较 forget/shared-retain/real-person-retain 的 cloze、classification、generation VQA/QA 指标，以及每个 epoch 的 `M_mul`、`M_uni`、`gap_ema`、gamma 范围和撞边界比例。

## 9. 评测与文件协议

每个 epoch 使用 vLLM 单卡评测：

```text
batch_size = 32
tensor_parallel_size = 1
max_model_len = 4096
max_new_tokens = 50
temperature = 0
```

LoRA 在评测前临时 merge 到 `llava_smu_ft`；结束后由 Python cleanup 和 shell 双重清理 merged model。目录结构：

```text
results/MAW/runs/<run-id>/
  config/args.json
  config/controller_state.json
  adapters/epochs/epoch-N/
  adapters/final -> epochs/epoch-5
  logs/train.log
  logs/eval/epoch-N.log
  logs/tensorboard/
  metrics/epoch-N/
```

总索引为 `results/MAW/experiments.tsv`，`results/MAW/latest` 指向最新 run；中断实验位于 `results/MAW/incomplete/`。

## 10. 已知注意点

1. 历史 run 的 `args.json` 仍记录迁移前的旧 run_dir 字符串，但文件已迁入新目录，模型参数不受影响。
2. 多卡文本日志中的 tqdm 和 epoch summary 会由 rank 重复输出；TensorBoard 仅 rank 0 写入。
3. 当前 DDP 使用 `find_unused_parameters=True`，日志显示 forward 未发现 unused parameters，会带来少量额外开销。
4. retain collator 当前没有显式传 `max_length`，日志曾提示 truncation 没有最大长度；forget collator 已显式使用 1024。后续应统一 retain 截断策略。
5. 必须显式记录全部 gamma 参数。只写“dynamic gamma”或“1:1”不足以复现。
