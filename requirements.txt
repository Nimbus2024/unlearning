# UMU-Bench 核心依赖
# 注意: torch 版本由服务器 GPU/CUDA 决定(如 AutoDL RTX 5090 需 torch>=2.4 cu128),
# 不在本文件锁定; 建议在环境里单独装匹配的 torch。
torch
torchvision
transformers
accelerate
peft
datasets
bitsandbytes
pandas
pyarrow
Pillow
nltk
rouge-score
scikit-learn
tqdm
tensorboard
sentencepiece
